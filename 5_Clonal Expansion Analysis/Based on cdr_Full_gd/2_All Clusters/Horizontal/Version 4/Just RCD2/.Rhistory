scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "αβ Clonal Size Distribution per Cluster in RCD-I (Raw Counts)"
) +
white_theme
ggsave("CloneSize_ab_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/5_Clonal Expansion Analysis/Based on cdr_Full_ab/2_All Clusters/Horizontal/Version 5/Just RCD2")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
# if (!require("BiocManager", quietly = TRUE))
#   install.packages("BiocManager")
# BiocManager::install("scRepertoire")
# suppressMessages(library(scRepertoire))
library(dplyr)
library(tidyr)
library(ggplot2)
full_metadata <- full_metadata[full_metadata$Diagnosis == "RCD-II", ]
target_clusters <- c(
"Plasma cells_1", "B cells_1", "B cells_2",
"B cells MZB1+", "Plasma cells_2", "Plasmablast",
"B cells BAFFR", "DC", "Macrophages", "Mast cells"
)
metadata_filtered <- full_metadata %>%
filter(!cluster %in% target_clusters)
metadata_filtered <- metadata_filtered %>%
mutate(clone_category = case_when(
clone_size_ab == 1 ~ "Singleton",
clone_size_ab >= 2 & clone_size_ab <= 10 ~ "Size 2-10",
clone_size_ab >= 11 & clone_size_ab <= 50 ~ "Size 11-50",
clone_size_ab >= 51 & clone_size_ab <= 100 ~ "Size 51-100",
clone_size_ab >= 101 ~ "Size 100+",
TRUE ~ NA_character_
))
df <- metadata_filtered %>%
filter(!is.na(clone_category)) %>%
group_by(cluster, clone_category) %>%
summarise(n_cells = n(), .groups = "drop") %>%
complete(cluster, clone_category = c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+"),
fill = list(n_cells = 0)) %>%
group_by(cluster) %>%
mutate(perc = if (sum(n_cells) == 0) 0 else n_cells / sum(n_cells) * 100) %>%
ungroup()
df$clone_category <- factor(df$clone_category, levels = rev(c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+")))
df$cluster <- factor(df$cluster)
white_theme <- theme_minimal(base_size = 12) +
theme(
plot.background = element_rect(fill = "white", color = NA),
panel.background = element_rect(fill = "white", color = NA),
legend.background = element_rect(fill = "white", color = NA),
axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
axis.text.y = element_text(size = 8),
plot.title = element_text(size = 12, face = "bold", hjust = 0.5)
)
# clone_colors <- rev(c("#1B263B", "#2E4057", "#355C7D", "#3A7BD5", "#1E90FF"))
clone_colors <- rev(c("#B3B3B3", "#F0CCFF", "#F8766D", "#619CFF", "#F032E6"))
p1 <- ggplot(df, aes(x = cluster, y = perc, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Percentage of Cells", fill = "Clone Size",
title = "αβ Clonal Size Distribution per Cluster in RCD-II (Percentage)"
) +
white_theme
ggsave("CloneSize_ab_Percentage.png", p1, width = 10, height = 5, dpi = 400, bg = "white")
p2 <- ggplot(df, aes(x = cluster, y = n_cells, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "αβ Clonal Size Distribution per Cluster in RCD-II (Raw Counts)"
) +
white_theme
ggsave("CloneSize_ab_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/5_Clonal Expansion Analysis/Based on cdr_Full_gd/2_All Clusters/Horizontal/Version 4/Just ACD")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
# if (!require("BiocManager", quietly = TRUE))
#  install.packages("BiocManager")
# BiocManager::install("scRepertoire")
# suppressMessages(library(scRepertoire))
library(dplyr)
library(tidyr)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggplot2)
full_metadata <- full_metadata[full_metadata$Diagnosis == "ACD", ]
target_clusters <- c(
"Plasma cells_1", "B cells_1", "B cells_2",
"B cells MZB1+", "Plasma cells_2", "Plasmablast",
"B cells BAFFR", "DC", "Macrophages", "Mast cells"
)
metadata_filtered <- full_metadata %>%
filter(!cluster %in% target_clusters)
metadata_filtered <- metadata_filtered %>%
mutate(clone_category = case_when(
clone_size_gd == 1 ~ "Singleton",
clone_size_gd >= 2 & clone_size_gd <= 10 ~ "Size 2-10",
clone_size_gd >= 11 & clone_size_gd <= 50 ~ "Size 11-50",
clone_size_gd >= 51 & clone_size_gd <= 100 ~ "Size 51-100",
clone_size_gd >= 101 ~ "Size 100+",
TRUE ~ NA_character_
))
df <- metadata_filtered %>%
filter(!is.na(clone_category)) %>%
group_by(cluster, clone_category) %>%
summarise(n_cells = n(), .groups = "drop") %>%
complete(cluster, clone_category = c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+"),
fill = list(n_cells = 0)) %>%
group_by(cluster) %>%
mutate(perc = if (sum(n_cells) == 0) 0 else n_cells / sum(n_cells) * 100) %>%
ungroup()
df$clone_category <- factor(df$clone_category, levels = rev(c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+")))
df$cluster <- factor(df$cluster)
white_theme <- theme_minimal(base_size = 12) +
theme(
plot.background = element_rect(fill = "white", color = NA),
panel.background = element_rect(fill = "white", color = NA),
legend.background = element_rect(fill = "white", color = NA),
axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
axis.text.y = element_text(size = 8),
plot.title = element_text(size = 12, face = "bold", hjust = 0.5)
)
# clone_colors <- rev(c("#1B263B", "#2E4057", "#355C7D", "#3A7BD5", "#1E90FF"))
clone_colors <- rev(c("#B3B3B3", "#F0CCFF", "#F8766D", "#619CFF", "#F032E6"))
p1 <- ggplot(df, aes(x = cluster, y = perc, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Percentage of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in ACD (Percentage)"
) +
white_theme
ggsave("CloneSize_gd_Percentage.png", p1, width = 10, height = 5, dpi = 400, bg = "white")
p2 <- ggplot(df, aes(x = cluster, y = n_cells, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in ACD (Raw Counts)"
) +
white_theme
ggsave("CloneSize_gd_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/5_Clonal Expansion Analysis/Based on cdr_Full_gd/2_All Clusters/Horizontal/Version 4/Just Healthy")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
# if (!require("BiocManager", quietly = TRUE))
#  install.packages("BiocManager")
# BiocManager::install("scRepertoire")
# suppressMessages(library(scRepertoire))
library(dplyr)
library(tidyr)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggplot2)
full_metadata <- full_metadata[full_metadata$Diagnosis == "Healthy", ]
target_clusters <- c(
"Plasma cells_1", "B cells_1", "B cells_2",
"B cells MZB1+", "Plasma cells_2", "Plasmablast",
"B cells BAFFR", "DC", "Macrophages", "Mast cells"
)
metadata_filtered <- full_metadata %>%
filter(!cluster %in% target_clusters)
metadata_filtered <- metadata_filtered %>%
mutate(clone_category = case_when(
clone_size_gd == 1 ~ "Singleton",
clone_size_gd >= 2 & clone_size_gd <= 10 ~ "Size 2-10",
clone_size_gd >= 11 & clone_size_gd <= 50 ~ "Size 11-50",
clone_size_gd >= 51 & clone_size_gd <= 100 ~ "Size 51-100",
clone_size_gd >= 101 ~ "Size 100+",
TRUE ~ NA_character_
))
df <- metadata_filtered %>%
filter(!is.na(clone_category)) %>%
group_by(cluster, clone_category) %>%
summarise(n_cells = n(), .groups = "drop") %>%
complete(cluster, clone_category = c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+"),
fill = list(n_cells = 0)) %>%
group_by(cluster) %>%
mutate(perc = if (sum(n_cells) == 0) 0 else n_cells / sum(n_cells) * 100) %>%
ungroup()
df$clone_category <- factor(df$clone_category, levels = rev(c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+")))
df$cluster <- factor(df$cluster)
white_theme <- theme_minimal(base_size = 12) +
theme(
plot.background = element_rect(fill = "white", color = NA),
panel.background = element_rect(fill = "white", color = NA),
legend.background = element_rect(fill = "white", color = NA),
axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
axis.text.y = element_text(size = 8),
plot.title = element_text(size = 12, face = "bold", hjust = 0.5)
)
# clone_colors <- rev(c("#1B263B", "#2E4057", "#355C7D", "#3A7BD5", "#1E90FF"))
clone_colors <- rev(c("#B3B3B3", "#F0CCFF", "#F8766D", "#619CFF", "#F032E6"))
p1 <- ggplot(df, aes(x = cluster, y = perc, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Percentage of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in Healthy (Percentage)"
) +
white_theme
ggsave("CloneSize_gd_Percentage.png", p1, width = 10, height = 5, dpi = 400, bg = "white")
p2 <- ggplot(df, aes(x = cluster, y = n_cells, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in Healthy (Raw Counts)"
) +
white_theme
ggsave("CloneSize_gd_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/5_Clonal Expansion Analysis/Based on cdr_Full_gd/2_All Clusters/Horizontal/Version 4/Just RCD1")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
# if (!require("BiocManager", quietly = TRUE))
#  install.packages("BiocManager")
# BiocManager::install("scRepertoire")
# suppressMessages(library(scRepertoire))
library(dplyr)
library(tidyr)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggplot2)
full_metadata <- full_metadata[full_metadata$Diagnosis == "RCD-I", ]
target_clusters <- c(
"Plasma cells_1", "B cells_1", "B cells_2",
"B cells MZB1+", "Plasma cells_2", "Plasmablast",
"B cells BAFFR", "DC", "Macrophages", "Mast cells"
)
metadata_filtered <- full_metadata %>%
filter(!cluster %in% target_clusters)
metadata_filtered <- metadata_filtered %>%
mutate(clone_category = case_when(
clone_size_gd == 1 ~ "Singleton",
clone_size_gd >= 2 & clone_size_gd <= 10 ~ "Size 2-10",
clone_size_gd >= 11 & clone_size_gd <= 50 ~ "Size 11-50",
clone_size_gd >= 51 & clone_size_gd <= 100 ~ "Size 51-100",
clone_size_gd >= 101 ~ "Size 100+",
TRUE ~ NA_character_
))
df <- metadata_filtered %>%
filter(!is.na(clone_category)) %>%
group_by(cluster, clone_category) %>%
summarise(n_cells = n(), .groups = "drop") %>%
complete(cluster, clone_category = c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+"),
fill = list(n_cells = 0)) %>%
group_by(cluster) %>%
mutate(perc = if (sum(n_cells) == 0) 0 else n_cells / sum(n_cells) * 100) %>%
ungroup()
df$clone_category <- factor(df$clone_category, levels = rev(c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+")))
df$cluster <- factor(df$cluster)
white_theme <- theme_minimal(base_size = 12) +
theme(
plot.background = element_rect(fill = "white", color = NA),
panel.background = element_rect(fill = "white", color = NA),
legend.background = element_rect(fill = "white", color = NA),
axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
axis.text.y = element_text(size = 8),
plot.title = element_text(size = 12, face = "bold", hjust = 0.5)
)
# clone_colors <- rev(c("#1B263B", "#2E4057", "#355C7D", "#3A7BD5", "#1E90FF"))
clone_colors <- rev(c("#B3B3B3", "#F0CCFF", "#F8766D", "#619CFF", "#F032E6"))
p1 <- ggplot(df, aes(x = cluster, y = perc, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Percentage of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in RCD-I (Percentage)"
) +
white_theme
ggsave("CloneSize_gd_Percentage.png", p1, width = 10, height = 5, dpi = 400, bg = "white")
p2 <- ggplot(df, aes(x = cluster, y = n_cells, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in RCD-I (Raw Counts)"
) +
white_theme
ggsave("CloneSize_gd_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/5_Clonal Expansion Analysis/Based on cdr_Full_gd/2_All Clusters/Horizontal/Version 4/Just RCD2")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
# if (!require("BiocManager", quietly = TRUE))
#  install.packages("BiocManager")
# BiocManager::install("scRepertoire")
# suppressMessages(library(scRepertoire))
library(dplyr)
library(tidyr)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggplot2)
full_metadata <- full_metadata[full_metadata$Diagnosis == "RCD-II", ]
target_clusters <- c(
"Plasma cells_1", "B cells_1", "B cells_2",
"B cells MZB1+", "Plasma cells_2", "Plasmablast",
"B cells BAFFR", "DC", "Macrophages", "Mast cells"
)
metadata_filtered <- full_metadata %>%
filter(!cluster %in% target_clusters)
metadata_filtered <- metadata_filtered %>%
mutate(clone_category = case_when(
clone_size_gd == 1 ~ "Singleton",
clone_size_gd >= 2 & clone_size_gd <= 10 ~ "Size 2-10",
clone_size_gd >= 11 & clone_size_gd <= 50 ~ "Size 11-50",
clone_size_gd >= 51 & clone_size_gd <= 100 ~ "Size 51-100",
clone_size_gd >= 101 ~ "Size 100+",
TRUE ~ NA_character_
))
df <- metadata_filtered %>%
filter(!is.na(clone_category)) %>%
group_by(cluster, clone_category) %>%
summarise(n_cells = n(), .groups = "drop") %>%
complete(cluster, clone_category = c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+"),
fill = list(n_cells = 0)) %>%
group_by(cluster) %>%
mutate(perc = if (sum(n_cells) == 0) 0 else n_cells / sum(n_cells) * 100) %>%
ungroup()
df$clone_category <- factor(df$clone_category, levels = rev(c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+")))
df$cluster <- factor(df$cluster)
white_theme <- theme_minimal(base_size = 12) +
theme(
plot.background = element_rect(fill = "white", color = NA),
panel.background = element_rect(fill = "white", color = NA),
legend.background = element_rect(fill = "white", color = NA),
axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
axis.text.y = element_text(size = 8),
plot.title = element_text(size = 12, face = "bold", hjust = 0.5)
)
# clone_colors <- rev(c("#1B263B", "#2E4057", "#355C7D", "#3A7BD5", "#1E90FF"))
clone_colors <- rev(c("#B3B3B3", "#F0CCFF", "#F8766D", "#619CFF", "#F032E6"))
p1 <- ggplot(df, aes(x = cluster, y = perc, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Percentage of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in RCD-II (Percentage)"
) +
white_theme
ggsave("CloneSize_gd_Percentage.png", p1, width = 10, height = 5, dpi = 400, bg = "white")
p2 <- ggplot(df, aes(x = cluster, y = n_cells, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in RCD-II (Raw Counts)"
) +
white_theme
ggsave("CloneSize_gd_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/1_Data Cleaning")
load("~/Downloads/MetaData_jerome.Rdata")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
unique(full_metadata$imm_receptor_Esmaeil)
View(full_metadata)
subset_data <- subset(full_metadata, imm_receptor_Esmaeil == "Aberrant g")
subset_data$cdr_Full_gd <- ifelse(
is.na(subset_data$g_cdr3) | is.na(subset_data$d_cdr3),
NA,
paste(subset_data$g_cdr3, subset_data$d_cdr3, sep = "")
)
full_metadata$cdr_Full_gd <- NA
full_metadata$cdr_Full_gd[full_metadata$imm_receptor_Esmaeil == "Aberrant g"] <- ifelse(
is.na(full_metadata$g_cdr3[full_metadata$imm_receptor_Esmaeil == "Aberrant g"]) |
is.na(full_metadata$d_cdr3[full_metadata$imm_receptor_Esmaeil == "Aberrant g"]),
NA,
paste(
full_metadata$g_cdr3[full_metadata$imm_receptor_Esmaeil == "Aberrant g"],
full_metadata$d_cdr3[full_metadata$imm_receptor_Esmaeil == "Aberrant g"],
sep = ""
)
)
full_metadata$cdr_Full_gd <- NA
# شرط: فقط سطرهایی که Aberrant g هستند و NA هم نیستند
idx <- !is.na(full_metadata$imm_receptor_Esmaeil) & full_metadata$imm_receptor_Esmaeil == "Aberrant g"
# حالا ساختن ستون
full_metadata$cdr_Full_gd[idx] <- ifelse(
is.na(full_metadata$g_cdr3[idx]) | is.na(full_metadata$d_cdr3[idx]),
NA,
paste(full_metadata$g_cdr3[idx], full_metadata$d_cdr3[idx], sep = "")
)
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
subset_data <- subset(full_metadata, imm_receptor_Esmaeil == "Aberrant g")
subset_data$cdr_Full_gd <- ifelse(
is.na(subset_data$g_cdr3) | is.na(subset_data$d_cdr3),
NA,
paste(subset_data$g_cdr3, subset_data$d_cdr3, sep = "")
)
View(subset_data)
# ایندکس سطرهایی که imm_receptor_Esmaeil == "Aberrant g"
idx <- !is.na(full_metadata$imm_receptor_Esmaeil) &
full_metadata$imm_receptor_Esmaeil == "Aberrant g"
# ایندکس سطرهایی که imm_receptor_Esmaeil == "Aberrant g"
idx <- !is.na(full_metadata$imm_receptor_Esmaeil) &
full_metadata$imm_receptor_Esmaeil == "Aberrant g"
# ساخت یا به‌روزرسانی ستون فقط برای همین سطرها
full_metadata$cdr_Full_gd[idx] <- ifelse(
is.na(full_metadata$g_cdr3[idx]) | is.na(full_metadata$d_cdr3[idx]),
NA,
paste(full_metadata$g_cdr3[idx], full_metadata$d_cdr3[idx], sep = "")
)
View(full_metadata)
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
View(full_metadata)
idx <- full_metadata$imm_receptor_Esmaeil == "Aberrant g"
full_metadata$cdr_Full_gd[idx] <- ifelse(
!is.na(full_metadata$g_cdr3[idx]) & !is.na(full_metadata$d_cdr3[idx]),
paste(full_metadata$g_cdr3[idx], full_metadata$d_cdr3[idx], sep = "+"),
NA
)
# شرط تمیز (فقط TRUE/FALSE بدون NA)
idx <- !is.na(full_metadata$imm_receptor_Esmaeil) & full_metadata$imm_receptor_Esmaeil == "Aberrant g"
# فقط اگر هر دو ستون مقدار داشتن، مقدار بساز
full_metadata$cdr_Full_gd[idx] <- ifelse(
!is.na(full_metadata$g_cdr3[idx]) & !is.na(full_metadata$d_cdr3[idx]),
paste(full_metadata$g_cdr3[idx], full_metadata$d_cdr3[idx], sep = "+"),
NA
)
unique(full_metadata$cdr_Full_gd)
View(full_metadata)
valid_idx <- !is.na(full_metadata$cdr_Full_gd)
cdr_counts <- table(full_metadata$cdr_Full_gd[valid_idx])
full_metadata$clone_size_gd[valid_idx] <- cdr_counts[full_metadata$cdr_Full_gd[valid_idx]]
View(full_metadata)
full_metadata$clone_size_gd[full_metadata$imm_receptor_Esmaeil == "Aberrant g"] <- NA
load("~/Downloads/MetaData_Esmaeil (1).Rdata")
full_metadata$clone_size_gd[full_metadata$imm_receptor_Esmaeil == "Aberrant g"] <- NA
# شرط تمیز (فقط TRUE/FALSE بدون NA)
idx <- !is.na(full_metadata$imm_receptor_Esmaeil) & full_metadata$imm_receptor_Esmaeil == "Aberrant g"
# فقط اگر هر دو ستون مقدار داشتن، مقدار بساز
full_metadata$cdr_Full_gd[idx] <- ifelse(
!is.na(full_metadata$g_cdr3[idx]) & !is.na(full_metadata$d_cdr3[idx]),
paste(full_metadata$g_cdr3[idx], full_metadata$d_cdr3[idx], sep = "+"),
NA
)
valid_idx <- !is.na(full_metadata$cdr_Full_gd)
# table بسازیم
cdr_counts <- table(full_metadata$cdr_Full_gd[valid_idx])
# برای هر مقدار، تعدادش رو بریزیم تو ستون جدید
full_metadata$clone_size_gd[valid_idx] <- cdr_counts[full_metadata$cdr_Full_gd[valid_idx]]
View(full_metadata)
getwd()
saveRDS(full_metadata, file = "full_metadata.rds")
setwd("C:/Users/Esmaeil/Desktop/Celiac/CeliacProject/5_Clonal Expansion Analysis/Based on cdr_Full_gd/2_All Clusters/Horizontal/Version 4/Just RCD2")
full_metadata <- readRDS("~/Downloads/full_metadata.rds")
# if (!require("BiocManager", quietly = TRUE))
#  install.packages("BiocManager")
# BiocManager::install("scRepertoire")
# suppressMessages(library(scRepertoire))
library(dplyr)
library(tidyr)
library(ggplot2)
library(dplyr)
library(tidyr)
library(ggplot2)
full_metadata <- full_metadata[full_metadata$Diagnosis == "RCD-II", ]
target_clusters <- c(
"Plasma cells_1", "B cells_1", "B cells_2",
"B cells MZB1+", "Plasma cells_2", "Plasmablast",
"B cells BAFFR", "DC", "Macrophages", "Mast cells"
)
metadata_filtered <- full_metadata %>%
filter(!cluster %in% target_clusters)
metadata_filtered <- metadata_filtered %>%
mutate(clone_category = case_when(
clone_size_gd == 1 ~ "Singleton",
clone_size_gd >= 2 & clone_size_gd <= 10 ~ "Size 2-10",
clone_size_gd >= 11 & clone_size_gd <= 50 ~ "Size 11-50",
clone_size_gd >= 51 & clone_size_gd <= 100 ~ "Size 51-100",
clone_size_gd >= 101 ~ "Size 100+",
TRUE ~ NA_character_
))
df <- metadata_filtered %>%
filter(!is.na(clone_category)) %>%
group_by(cluster, clone_category) %>%
summarise(n_cells = n(), .groups = "drop") %>%
complete(cluster, clone_category = c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+"),
fill = list(n_cells = 0)) %>%
group_by(cluster) %>%
mutate(perc = if (sum(n_cells) == 0) 0 else n_cells / sum(n_cells) * 100) %>%
ungroup()
df$clone_category <- factor(df$clone_category, levels = rev(c("Singleton", "Size 2-10", "Size 11-50", "Size 51-100", "Size 100+")))
df$cluster <- factor(df$cluster)
white_theme <- theme_minimal(base_size = 12) +
theme(
plot.background = element_rect(fill = "white", color = NA),
panel.background = element_rect(fill = "white", color = NA),
legend.background = element_rect(fill = "white", color = NA),
axis.text.x = element_text(angle = 45, hjust = 1, size = 8),
axis.text.y = element_text(size = 8),
plot.title = element_text(size = 12, face = "bold", hjust = 0.5)
)
# clone_colors <- rev(c("#1B263B", "#2E4057", "#355C7D", "#3A7BD5", "#1E90FF"))
clone_colors <- rev(c("#B3B3B3", "#F0CCFF", "#F8766D", "#619CFF", "#F032E6"))
p1 <- ggplot(df, aes(x = cluster, y = perc, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Percentage of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in RCD-II (Percentage)"
) +
white_theme
ggsave("CloneSize_gd_Percentage.png", p1, width = 10, height = 5, dpi = 400, bg = "white")
p2 <- ggplot(df, aes(x = cluster, y = n_cells, fill = clone_category)) +
geom_bar(stat = "identity") +
scale_fill_manual(values = clone_colors) +
labs(
x = "Cluster", y = "Number of Cells", fill = "Clone Size",
title = "γδ Clonal Size Distribution per Cluster in RCD-II (Raw Counts)"
) +
white_theme
ggsave("CloneSize_gd_RawCounts.png", p2, width = 10, height = 5, dpi = 400, bg = "white")
